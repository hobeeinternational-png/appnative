import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { router, useLocalSearchParams } from "expo-router";
import { Image, Pressable, ScrollView, StyleSheet, Text, View } from "react-native";

import { AppStatusState } from "@/components/hobee/error-screens";
import { ScreenContainer } from "@/components/screen-container";
import { LEARNING_EVENTS, LEARNING_LIVE_CLASSES, LEARNING_ONE_ON_ONE_SERVICES, TEACHER_LEARNER_BRIDGES, coursesForTeacher, getLearningTeacher } from "@/lib/learning-platform";

export default function LearningTeacherScreen() { const { id } = useLocalSearchParams<{ id: string }>(); const teacher = getLearningTeacher(id); if (!teacher) return <ScreenContainer><AppStatusState title="ไม่พบข้อมูลผู้สอน" description="ผู้สอนรายนี้อาจยังไม่เปิดเผยใน Learning catalogue" actionLabel="กลับ Learning" onAction={() => router.replace("/learn")} /></ScreenContainer>; const courses = coursesForTeacher(teacher.id); const bridge = TEACHER_LEARNER_BRIDGES.find((item) => item.teacherId === teacher.id); const events = LEARNING_EVENTS.filter((item) => item.teacherId === teacher.id); const live = LEARNING_LIVE_CLASSES.filter((item) => item.teacherId === teacher.id); const services = LEARNING_ONE_ON_ONE_SERVICES.filter((item) => item.teacherId === teacher.id); return <ScreenContainer edges={["top", "left", "right", "bottom"]} containerClassName="bg-[#0C0D0D]" safeAreaClassName="bg-[#0C0D0D]"><View style={styles.screen}><ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}><View style={styles.cover}><Image source={{ uri: teacher.cover }} style={styles.coverImage} /><View style={styles.coverShade} /><Pressable onPress={() => router.canGoBack() ? router.back() : router.replace("/learn")} style={styles.back}><MaterialIcons name="arrow-back" size={23} color="#F7F4ED" /></Pressable></View><View style={styles.profile}><Image source={{ uri: teacher.avatar }} style={styles.avatar} /><Text style={styles.name}>{teacher.name}</Text><Text style={styles.title}>{teacher.title}</Text><Text style={styles.bio}>{teacher.bio}</Text><View style={styles.tags}>{teacher.expertise.map((item) => <View key={item} style={styles.tag}><Text style={styles.tagText}>{item}</Text></View>)}</View><View style={styles.stats}><Stat icon="star" label="rating" value={teacher.rating.toFixed(1)} /><Stat icon="rate-review" label="reviews" value={`${teacher.reviewsCount}`} /><Stat icon="menu-book" label="courses" value={`${courses.length}`} /></View></View><Section title="คอร์สจากผู้สอน" />{courses.map((course) => <Pressable key={course.id} onPress={() => router.push({ pathname: "/learning/[id]", params: { id: course.id } } as never)} style={({ pressed }) => [styles.course, pressed && styles.pressed]}><Text style={styles.courseTitle}>{course.title}</Text><Text style={styles.courseMeta}>{course.episodesCount} บทเรียน • {course.category}</Text><MaterialIcons name="chevron-right" size={20} color="#AAA7A1" /></Pressable>)}{live.length || events.length ? <><Section title="Live และกิจกรรม" />{[...live, ...events].map((item) => <View key={item.id} style={styles.activity}><MaterialIcons name={"durationMinutes" in item ? "live-tv" : "event"} size={19} color="#E2BA48" /><Text style={styles.activityTitle}>{item.title}</Text></View>)}</> : null}{services.length ? <><Section title="1-on-1" />{services.map((service) => <Pressable key={service.id} onPress={() => router.push(`/learning/sessions/${service.id}` as never)} style={({ pressed }) => [styles.service, pressed && styles.pressed]}><MaterialIcons name="person" size={20} color="#90CFA0" /><View style={styles.serviceCopy}><Text style={styles.serviceTitle}>{service.title}</Text><Text style={styles.serviceMeta}>{service.durationMinutes} นาที • {service.availabilityLabel}</Text></View><MaterialIcons name="chevron-right" size={20} color="#AAA7A1" /></Pressable>)}</> : null}<View style={styles.bridge}><MaterialIcons name="hub" size={18} color="#E2BA48" /><Text style={styles.bridgeText}>Teacher-to-Learner bridge: progress จาก {bridge?.learnerProgressSource === "local_progress" ? "local learning progress" : "backend"}; enrollment และ publishing อยู่ใน presentation boundary</Text></View></ScrollView></View></ScreenContainer>; }
function Section({ title }: { title: string }) { return <Text style={styles.section}>{title}</Text>; } function Stat({ icon, label, value }: { icon: keyof typeof MaterialIcons.glyphMap; label: string; value: string }) { return <View style={styles.stat}><MaterialIcons name={icon} size={17} color="#E0B747" /><Text style={styles.statValue}>{value}</Text><Text style={styles.statLabel}>{label}</Text></View>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: "#0C0D0D" }, content: { paddingBottom: 42 }, cover: { height: 188 }, coverImage: { width: "100%", height: "100%" }, coverShade: { ...StyleSheet.absoluteFillObject, backgroundColor: "rgba(5,5,5,0.38)" }, back: { position: "absolute", top: 12, left: 15, width: 38, height: 38, alignItems: "center", justifyContent: "center", borderRadius: 12, backgroundColor: "rgba(0,0,0,0.46)" }, profile: { alignItems: "center", marginTop: -45, paddingHorizontal: 22 }, avatar: { width: 84, height: 84, borderRadius: 42, borderWidth: 3, borderColor: "#E0B747" }, name: { color: "#F6F3EC", fontSize: 21, fontWeight: "900", marginTop: 10 }, title: { color: "#E1B949", fontSize: 11, fontWeight: "800", marginTop: 4 }, bio: { color: "#C2BFB7", fontSize: 11, fontWeight: "600", lineHeight: 17, textAlign: "center", marginTop: 11 }, tags: { flexDirection: "row", flexWrap: "wrap", justifyContent: "center", gap: 7, marginTop: 12 }, tag: { borderRadius: 10, backgroundColor: "#1B1D1B", paddingHorizontal: 9, paddingVertical: 6 }, tagText: { color: "#D7D3CA", fontSize: 10, fontWeight: "800" }, stats: { flexDirection: "row", width: "100%", gap: 8, marginTop: 18 }, stat: { flex: 1, alignItems: "center", gap: 3, borderRadius: 12, borderWidth: 1, borderColor: "#30322F", backgroundColor: "#171918", paddingVertical: 10 }, statValue: { color: "#F6F3EC", fontSize: 12, fontWeight: "900" }, statLabel: { color: "#929089", fontSize: 8, fontWeight: "800" }, section: { color: "#F6F3EC", fontSize: 16, fontWeight: "900", marginLeft: 18, marginTop: 26, marginBottom: 10 }, course: { flexDirection: "row", alignItems: "center", gap: 8, marginHorizontal: 18, marginBottom: 8, borderRadius: 12, borderWidth: 1, borderColor: "#30312F", backgroundColor: "#171918", padding: 12 }, courseTitle: { flex: 1, color: "#ECE9E1", fontSize: 11, fontWeight: "900" }, courseMeta: { color: "#98958F", fontSize: 9, fontWeight: "700" }, activity: { flexDirection: "row", alignItems: "center", gap: 10, marginHorizontal: 18, marginBottom: 8, borderRadius: 11, backgroundColor: "#171918", padding: 11 }, activityTitle: { flex: 1, color: "#DBD8D0", fontSize: 11, fontWeight: "800" }, service: { flexDirection: "row", alignItems: "center", gap: 10, marginHorizontal: 18, marginBottom: 8, borderRadius: 12, borderWidth: 1, borderColor: "#364237", backgroundColor: "#171918", padding: 11 }, serviceCopy: { flex: 1 }, serviceTitle: { color: "#E9E6DF", fontSize: 11, fontWeight: "900" }, serviceMeta: { color: "#9E9B94", fontSize: 9, fontWeight: "600", marginTop: 3 }, bridge: { flexDirection: "row", alignItems: "flex-start", gap: 8, margin: 18, borderRadius: 12, backgroundColor: "#191A18", padding: 12 }, bridgeText: { flex: 1, color: "#AAA79F", fontSize: 10, fontWeight: "600", lineHeight: 15 }, pressed: { opacity: 0.78, transform: [{ scale: 0.98 }] } });
