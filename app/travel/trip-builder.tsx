import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { router } from "expo-router";
import { useMemo, useState } from "react";
import { Pressable, ScrollView, StyleSheet, Text, View } from "react-native";

import { BackHeader } from "@/components/hobee/commerce-ui";
import { HOBEE } from "@/components/hobee/design-tokens";
import { ScreenContainer } from "@/components/screen-container";
import { formatTravelPrice } from "@/lib/travel-data";

const DURATIONS = [{ id: "day", label: "1 วัน", nights: 0 }, { id: "weekend", label: "2 วัน 1 คืน", nights: 1 }, { id: "extended", label: "3 วัน 2 คืน", nights: 2 }] as const;
const BUDGETS = [{ id: "value", label: "ประหยัด", perDay: 1400, icon: "savings" }, { id: "comfort", label: "ปานกลาง", perDay: 2600, icon: "hotel" }, { id: "premium", label: "พรีเมียม", perDay: 4800, icon: "workspace-premium" }] as const;
const STYLES = [{ id: "slow", label: "สโลว์ไลฟ์", icon: "self-improvement", description: "ชิม ช้อป และใช้เวลาพักผ่อน" }, { id: "nature", label: "ลุยธรรมชาติ", icon: "forest", description: "วิวสวย เส้นทางเขียว และกิจกรรมกลางแจ้ง" }, { id: "halal", label: "เน้นกินฮาลาล", icon: "restaurant", description: "เมนูฮาลาลและชุมชนมุสลิมท้องถิ่น" }] as const;

export default function TripBuilderScreen() {
  const [duration, setDuration] = useState<(typeof DURATIONS)[number]["id"]>("weekend");
  const [budget, setBudget] = useState<(typeof BUDGETS)[number]["id"]>("comfort");
  const [style, setStyle] = useState<(typeof STYLES)[number]["id"]>("halal");
  const [generated, setGenerated] = useState(false);
  const selectedDuration = DURATIONS.find((item) => item.id === duration) ?? DURATIONS[1];
  const selectedBudget = BUDGETS.find((item) => item.id === budget) ?? BUDGETS[1];
  const selectedStyle = STYLES.find((item) => item.id === style) ?? STYLES[2];
  const total = selectedBudget.perDay * (selectedDuration.nights + 1);
  const plan = useMemo(() => selectedStyle.id === "halal" ? ["เริ่มเช้าที่ตลาดอาหารฮาลาล", "แวะชุมชนและร้านชาในพื้นที่", "มื้อเย็นพร้อมวิวท้องถิ่น"] : selectedStyle.id === "nature" ? ["ออกเดินทางสู่จุดชมวิว", "กิจกรรมธรรมชาติพร้อมไกด์ชุมชน", "พักผ่อนและชมพระอาทิตย์ตก"] : ["เดินเล่นย่านท้องถิ่น", "มื้อกลางวันและ Workshop ชุมชน", "คาเฟ่/ตลาดเย็นก่อนเดินทางกลับ"], [selectedStyle.id]);

  return <ScreenContainer edges={["top", "left", "right", "bottom"]} containerClassName="bg-[#FAFAF9]"><BackHeader title="ออกแบบทริปส่วนตัว" onBack={() => router.back()} /><ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.content}><View style={styles.hero}><Text style={styles.eyebrow}>CUSTOM TRIP BUILDER</Text><Text style={styles.title}>ทริปที่เหมาะกับ{`\n`}เวลาและสไตล์ของคุณ</Text><Text style={styles.copy}>เลือกเงื่อนไข 3 ข้อ แล้ว HOBEE จะจัดโครงแผนเที่ยวพร้อมงบประมาณเบื้องต้นให้</Text></View><OptionGroup title="คุณมีเวลาเท่าไร" options={DURATIONS} selected={duration} onSelect={setDuration} /><OptionGroup title="งบประมาณต่อทริป" options={BUDGETS} selected={budget} onSelect={setBudget} showIcon /><OptionGroup title="อยากเที่ยวแบบไหน" options={STYLES} selected={style} onSelect={setStyle} showDescription />
    <Pressable accessibilityRole="button" accessibilityLabel="สร้างแผนทริปส่วนตัว" onPress={() => setGenerated(true)} style={({ pressed }) => [styles.generateButton, pressed && styles.pressed]}><MaterialIcons name="auto-awesome" size={21} color={HOBEE.colors.darkBase} /><Text style={styles.generateText}>สร้างแผนทริปส่วนตัว</Text></Pressable>
    {generated ? <View style={styles.result}><View style={styles.resultTop}><View><Text style={styles.resultEyebrow}>HOBEE PLAN PREVIEW</Text><Text style={styles.resultTitle}>{selectedDuration.label} • {selectedStyle.label}</Text></View><MaterialIcons name="auto-awesome" size={27} color={HOBEE.colors.gold} /></View><Text style={styles.resultBudget}>ประมาณ {formatTravelPrice(total)} / คน</Text><View style={styles.timeline}>{plan.map((item, index) => <View key={item} style={styles.planItem}><View style={styles.planDot}><Text style={styles.planNumber}>{index + 1}</Text></View><Text style={styles.planText}>{item}</Text></View>)}</View><Pressable accessibilityRole="button" accessibilityLabel="ใช้แผนนี้และไปจอง" onPress={() => router.push({ pathname: "/travel/book/[id]", params: { id: style === "nature" ? "stay-betong-garden" : style === "slow" ? "trip-pattani-heritage" : "trip-lipe-3d2n" } } as never)} style={({ pressed }) => [styles.bookButton, pressed && styles.pressed]}><Text style={styles.bookText}>ใช้แผนนี้และไปจอง</Text><MaterialIcons name="arrow-forward" size={20} color="#FFFFFF" /></Pressable><Text style={styles.disclaimer}>เป็นแผน preview จากข้อมูลตัวอย่าง โปรดตรวจสอบราคาและวันว่างก่อนชำระเงินจริง</Text></View> : null}
  </ScrollView></ScreenContainer>;
}

function OptionGroup<T extends { id: string; label: string; icon?: keyof typeof MaterialIcons.glyphMap; description?: string }>({ title, options, selected, onSelect, showIcon = false, showDescription = false }: { title: string; options: readonly T[]; selected: string; onSelect: (id: T["id"]) => void; showIcon?: boolean; showDescription?: boolean }) {
  return <View style={styles.group}><Text style={styles.groupTitle}>{title}</Text><View style={showDescription ? styles.stackOptions : styles.rowOptions}>{options.map((option) => { const active = option.id === selected; return <Pressable key={option.id} accessibilityRole="button" accessibilityLabel={`เลือก ${option.label}`} onPress={() => onSelect(option.id)} style={({ pressed }) => [showDescription ? styles.styleOption : styles.option, active && (showDescription ? styles.styleOptionActive : styles.optionActive), pressed && styles.pressed]}>{showIcon && option.icon ? <MaterialIcons name={option.icon} size={20} color={active ? HOBEE.colors.darkBase : HOBEE.colors.goldDark} /> : null}{showDescription && option.icon ? <View style={[styles.styleIcon, active && styles.styleIconActive]}><MaterialIcons name={option.icon} size={21} color={active ? HOBEE.colors.darkBase : HOBEE.colors.travelTeal} /></View> : null}<View style={showDescription ? styles.styleCopy : undefined}><Text style={[styles.optionText, active && styles.optionTextActive]}>{option.label}</Text>{showDescription ? <Text style={[styles.optionDescription, active && styles.optionDescriptionActive]}>{option.description}</Text> : null}</View>{active ? <MaterialIcons name="check-circle" size={20} color={showDescription ? HOBEE.colors.gold : HOBEE.colors.darkBase} /> : null}</Pressable>; })}</View></View>;
}

const styles = StyleSheet.create({ content: { padding: 20, paddingBottom: 38, backgroundColor: HOBEE.colors.canvas }, hero: { borderRadius: HOBEE.radius.xlarge, backgroundColor: HOBEE.colors.darkCard, padding: 22 }, eyebrow: { color: HOBEE.colors.gold, fontSize: 10, fontWeight: "900", letterSpacing: 1 }, title: { color: "#FFFFFF", fontSize: HOBEE.type.h1, fontWeight: "900", lineHeight: 31, marginTop: 8 }, copy: { color: "#D6D3D1", fontSize: 13, fontWeight: "600", lineHeight: 19, marginTop: 9 }, group: { marginTop: 25 }, groupTitle: { color: HOBEE.colors.ink, fontSize: HOBEE.type.h3, fontWeight: "900", marginBottom: 11 }, rowOptions: { flexDirection: "row", gap: 8 }, stackOptions: { gap: 9 }, option: { flex: 1, minHeight: 56, alignItems: "center", justifyContent: "center", gap: 5, borderRadius: HOBEE.radius.small, borderWidth: 1, borderColor: HOBEE.colors.border, backgroundColor: HOBEE.colors.surface, padding: 8 }, optionActive: { borderColor: HOBEE.colors.gold, backgroundColor: "#FFF8E8" }, optionText: { color: HOBEE.colors.ink, fontSize: 12, fontWeight: "800", textAlign: "center" }, optionTextActive: { color: HOBEE.colors.darkBase }, styleOption: { flexDirection: "row", alignItems: "center", gap: 10, borderRadius: HOBEE.radius.medium, borderWidth: 1, borderColor: HOBEE.colors.border, backgroundColor: HOBEE.colors.surface, padding: 13 }, styleOptionActive: { borderColor: HOBEE.colors.gold, backgroundColor: "#FFF8E8" }, styleIcon: { width: 39, height: 39, alignItems: "center", justifyContent: "center", borderRadius: HOBEE.radius.small, backgroundColor: "#E8F7F1" }, styleIconActive: { backgroundColor: HOBEE.colors.gold }, styleCopy: { flex: 1 }, optionDescription: { color: HOBEE.colors.muted, fontSize: 11, fontWeight: "600", marginTop: 3 }, optionDescriptionActive: { color: "#6B5A31" }, generateButton: { height: 54, flexDirection: "row", alignItems: "center", justifyContent: "center", gap: 9, marginTop: 26, borderRadius: HOBEE.radius.small, backgroundColor: HOBEE.colors.gold }, generateText: { color: HOBEE.colors.darkBase, fontSize: 15, fontWeight: "900" }, result: { marginTop: 20, overflow: "hidden", borderRadius: HOBEE.radius.xlarge, borderWidth: 1, borderColor: "#EBC65E", backgroundColor: "#FFFDF5", padding: 18 }, resultTop: { flexDirection: "row", justifyContent: "space-between", alignItems: "flex-start" }, resultEyebrow: { color: HOBEE.colors.goldDark, fontSize: 10, fontWeight: "900", letterSpacing: 0.8 }, resultTitle: { color: HOBEE.colors.ink, fontSize: HOBEE.type.h2, fontWeight: "900", marginTop: 4 }, resultBudget: { color: HOBEE.colors.travelTeal, fontSize: 15, fontWeight: "900", marginTop: 12 }, timeline: { gap: 12, marginTop: 17 }, planItem: { flexDirection: "row", alignItems: "center", gap: 10 }, planDot: { width: 25, height: 25, alignItems: "center", justifyContent: "center", borderRadius: 13, backgroundColor: "#E8F7F1" }, planNumber: { color: HOBEE.colors.travelTeal, fontSize: 11, fontWeight: "900" }, planText: { flex: 1, color: HOBEE.colors.ink, fontSize: 13, fontWeight: "700" }, bookButton: { height: 47, flexDirection: "row", alignItems: "center", justifyContent: "center", gap: 7, marginTop: 20, borderRadius: HOBEE.radius.small, backgroundColor: HOBEE.colors.travelTeal }, bookText: { color: "#FFFFFF", fontSize: 13, fontWeight: "900" }, disclaimer: { color: HOBEE.colors.muted, fontSize: 10, fontWeight: "600", lineHeight: 15, textAlign: "center", marginTop: 10 }, pressed: { opacity: 0.8, transform: [{ scale: 0.98 }] } });
